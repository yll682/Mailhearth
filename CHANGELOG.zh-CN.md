# 更新日志

[English](CHANGELOG.md) · **简体中文**

本文件记录 Mailhearth 的所有重要改动。

格式参照 [Keep a Changelog](https://keepachangelog.com/en/1.1.0/)，版本号遵循
[语义化版本](https://semver.org/spec/v2.0.0.html)。

## [未发布]

## [0.1.0] - 2026-09-20

首个公开版本：把 Purelymail 账户变成团队邮件产品的自托管商务邮箱平台。

### 新增

- **首次运行向导**：创建组织、校验 Purelymail API 令牌，并导入现有的域名、邮箱
  和路由规则，不改动账户里的任何设置。导入过程只读取数据，可以重复执行。
- **组织模型**：成员带有头衔、部门、角色和状态（已邀请、活跃、已停用、
  已离职）；内置所有者、管理员和成员三种角色，也可以用细粒度权限组合出自定义
  角色；管理员操作留有审计日志，组织所有权可以转让。
- **邮箱生命周期**：个人邮箱和共享邮箱，权限分为完全访问、可读可发、只读；
  服务器为每个邮箱创建并轮换一个应用密码；一步完成离职交接——移交邮箱、转为
  共享、保留或锁定邮箱、转发新邮件、轮换全部凭据、把成员从群组里移除。
- **地址**：主地址、别名、转发、全收（catch-all）和前缀规则，以及自动跟随群组
  成员变化的分发地址。
- **域名**：域名管理，含 MX、SPF、DKIM、DMARC 的 DNS 健康状态，以及可直接复制
  的 DNS 记录。
- **网页邮箱**：文件夹、分页、服务端搜索、星标、批量操作、附件、自动保存的草稿、
  签名与多发件身份、基于 IMAP IDLE 的桌面通知、键盘快捷键、浅色与深色模式、
  中英文界面。
- **共享邮箱协作**：看到谁已回复、把邮件分配给某人、标记为已处理、留内部备注。
  协作状态以 `Message-ID` 为键，邮件在文件夹之间移动后依然保留。
- **规则**：结构化的条件和动作，加上假期自动回复，会编译成 Sieve 并通过
  ManageSieve 安装到服务器，没人登录时也照常生效。
- **安全**：Purelymail API 令牌和每个邮箱的应用密码使用 AES-256-GCM 加密存储，
  密钥由主密钥派生，绝不离开服务器；argon2id 密码哈希、CSRF 防护、登录限流；
  邮件 HTML 在服务端消毒，并在严格 CSP 下的无脚本沙箱 iframe 中渲染，远程图片
  默认拦截；附件带 `nosniff` 并强制下载。
- **部署**：内嵌网页客户端的单个静态 Go 二进制、基于纯 Go 驱动
  `modernc.org/sqlite` 的 SQLite 存储、`Dockerfile` 与 `docker-compose.yml`，
  以及一个 GitHub Actions 工作流：跑测试、构建二进制并把多架构镜像发布到 GHCR。
- **开发与验证**：进程内模拟的 Purelymail API、IMAP 和 SMTP 服务器，带演示数据
  （`make dev`）；驱动真实界面的截图脚本；对真实 Purelymail 账户运行的集成测试
  套件。

[未发布]: https://github.com/yll682/Mailhearth/compare/v0.1.0...HEAD
[0.1.0]: https://github.com/yll682/Mailhearth/releases/tag/v0.1.0
